# 视频源设置模块

## 概述

用户点击设置按钮时，弹出 NSMenu 选择源类型，根据选择显示对应的配置窗口。

## 交互流程

```
用户点击设置按钮 (settingButtonAction)
    ↓
弹出 NSMenu
    ├── 媒体源（本地视频） → WLMediaSourceSettingView
    └── 摄像头           → WLCameraSourceSettingView
    ↓ 用户确认
delegate 回调 → WLMainViewController 创建对应的 Source
```

## 文件结构

```
UI/Setting/
├── WLVideoDeviceSettingWindowController.h/.m   # 窗口控制器（单例）
├── WLVideoDeviceSettingView.h/.m               # 容器视图，根据 sourceType 切换子视图
├── WLCameraSourceSettingView.h/.m              # 摄像头配置视图
└── WLMediaSourceSettingView.h/.m               # 媒体源配置视图
```

## 核心类型

### WLVideoSourceType（定义在 WLVideoDeviceSettingView.h）

| 值 | 说明 |
|---|---|
| WLVideoSourceTypeMedia | 媒体源（本地视频文件） |
| WLVideoSourceTypeCamera | 摄像头采集 |

## 各层职责

### WLVideoDeviceSettingWindowController

- 单例模式（`sharedController`）
- 根据 sourceType 设置窗口标题
- 调用 `settingView.switchToSourceType:` 切换子视图
- 将子视图的 delegate 回调转发给外部 delegate

### WLVideoDeviceSettingView（容器）

- 持有 `WLCameraSourceSettingView` 和 `WLMediaSourceSettingView`
- `switchToSourceType:` 移除当前子视图，添加对应类型的子视图
- 实现两个子视图的 delegate，统一转发给自身的 delegate

### WLCameraSourceSettingView

- 预览区域（WLViedoPreview）
- 设备选择（NSComboBox，数据源为 WLDeviceItem）
- 预设复选框 + 预设选择（High / Medium / Low）
- 缓冲复选框
- 底部按钮（默认值 / 取消 / 确定）

### WLMediaSourceSettingView

- 文件路径输入框（NSTextField）
- 浏览按钮（NSOpenPanel，支持 mp4/mov/mkv/avi/flv/wmv/webm/m4v/ts/mts）
- 底部按钮（取消 / 确定）
- 空路径校验（弹窗提示）

## Delegate 链路

```
WLCameraSourceSettingView.delegate
    → WLVideoDeviceSettingView (转发)
        → WLVideoDeviceSettingWindowController (转发)
            → WLMainViewController

WLMediaSourceSettingView.delegate
    → WLVideoDeviceSettingView (转发)
        → WLVideoDeviceSettingWindowController (转发)
            → WLMainViewController
```

### WLVideoDeviceSettingWindowControllerDelegate

```objc
// 摄像头确认
- (void)videoDeviceSettingController:(WLVideoDeviceSettingWindowController *)controller
              didConfirmWithDevice:(NSString *)deviceID
                           preset:(NSString *)preset
                       useBuffer:(BOOL)useBuffer;

// 媒体源确认
- (void)videoDeviceSettingController:(WLVideoDeviceSettingWindowController *)controller
          didConfirmWithMediaPath:(NSString *)mediaPath;
```

## WLMainViewController 处理

- `settingButtonAction:` 弹出 NSMenu（菜单定位在按钮左上角）
- `mediaSourceSelected:` → 以 WLVideoSourceTypeMedia 模式弹出设置窗口
- `cameraSourceSelected:` → 以 WLVideoSourceTypeCamera 模式弹出设置窗口
- 摄像头回调：查找设备并切换 WLVideoManager
- 媒体源回调：创建 WLMediaSource 实例
