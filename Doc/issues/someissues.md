# 测试发现的现象清单

> ✅ **已全部修复（2026-06-15 核对）**：下列三条现象同源——根因是 **10-bit HEVC 源被当成 8-bit BGRA 绑定纹理**（滤镜激活走 Metal 离屏渲染时暴露）。`5d8ecd2` 修「分裂成左右两半」，`a75447c` 修「偏色/偏暗」（删 `is10Bit` 分支，P010 高位对齐可复用 8-bit 公式），均已实测通过。详见 [滤镜渲染问题排查.md](滤镜渲染问题排查.md)。原始现象记录保留如下供回溯。

1. ~~当修改颜色矫正或者裁剪的时候，渲染的视图总会分成左右两个部分~~ → ✅ 已修复（`5d8ecd2`）

2. ~~当裁剪的时候，颜色也不对了~~ → ✅ 已修复（`a75447c`）

3. ~~颜色矫正，比如滑动亮度，再次归零的时候，颜色不正常~~ → ✅ 已修复（`a75447c`）
