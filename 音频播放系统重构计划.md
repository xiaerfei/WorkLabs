# 音频播放系统重构计划

## Context

WorkLabs 项目需要一个完整的音频播放系统。当前存在的问题：

1. **音频播放链路不完整**：`WLAudioPlayer` 的渲染回调只输出静音，`WLMediaSource` 解码的音频帧没有传递给播放器
2. **缓冲区设计不合适**：现有的 `WLNodeQueue` 是为 `AVPacket/AVFrame` 设计的，不适合 PCM 音频数据流，缺少实时音频播放所需的环形缓冲区
3. **重采样器有缺陷**：`WLResample` 第256行存在严重问题 - 当输出缓冲区不足容纳重采样数据时，可能导致数据丢失或崩溃
4. **架构不灵活**：`WLAudioPlayer` 只能通过一种方式接收数据，不能同时服务 `WLMediaSource` 和作为独立播放器

用户要求：
1. `WLAudioPlayer` 为 `WLMediaSource` 服务，同时也能作为独立播放器
2. 重新设计缓冲区系统
3. 重构有问题的 `WLResample`

## 设计目标

1. **完整音频链路**：实现从 FFmpeg 解码到 AudioUnit 播放的完整数据流
2. **灵活架构**：支持三种使用模式：Delegate 模式（集成 WLMediaSource）、Buffer 模式（独立播放器）、Direct 模式（直接 PCM 数据）
3. **健壮重采样**：解决 `WLResample` 的缓冲区不足问题，支持安全的重采样操作
4. **实时性能**：低延迟播放，避免上溢/下溢，适合实时音频应用
5. **向后兼容**：保持现有接口，逐步迁移

## 整体架构

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  WLMediaSource  │────▶  WLAudioPlayer  │────▶  WLAudioBuffer  │────▶    AudioUnit    │
│  (解码器)       │    │  (播放控制器)   │    │  (环形缓冲区)   │    │  (系统输出)     │
└─────────────────┘    └─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │                       │
         └───────────────────────┼───────────────────────┘                       │
                                 │                                               │
                          ┌──────▼──────┐                                 ┌─────▼─────┐
                          │ WLResample  │                                 │ macOS     │
                          │ (重采样器)  │                                 │ CoreAudio │
                          └─────────────┘                                 └───────────┘
```

### 数据流路径

#### 模式1：WLMediaSource 集成模式
```
WLMediaSource.audioRenderThread
    → [delegate mediaSource:didOutputAudioFrame:pts:]
    → WLAudioPlayer.didReceiveAudioFrame:pts:
    → WLResample.resampleFrame: (如果需要格式转换)
    → WLAudioBuffer.writeData:
    → AudioUnit 回调从 WLAudioBuffer.readData:
    → 系统播放
```

#### 模式2：独立播放器模式
```
外部应用 → WLAudioPlayer.enqueuePCMData:
    → WLAudioBuffer.writeData:
    → AudioUnit 回调从 WLAudioBuffer.readData:
    → 系统播放
```

#### 模式3：直接代理模式
```
AudioUnit 回调 → [delegate audioPlayer:requestAudioData:length:timestamp:]
    → 代理直接提供 PCM 数据
    → 系统播放
```

## 关键组件设计

### 1. WLAudioPlayer (扩展)

**位置**: `WorkLabs/MediaSource/WLAudioPlayer.{h,m}`

**主要变更**：
1. 添加新的 `WLAudioPlayerDelegate` 协议，支持多种数据源
2. 添加三种播放模式枚举：`WLAudioPlayerModeDelegate`, `WLAudioPlayerModeBuffer`, `WLAudioPlayerModeDirect`
3. 集成 `WLAudioBuffer` 和 `WLResample`
4. 实现 `WLMediaSourceDelegate` 协议，支持与 `WLMediaSource` 集成
5. 添加播放状态监控和错误处理

**接口示例**：
```objective-c
typedef NS_ENUM(NSUInteger, WLAudioPlayerMode) {
    WLAudioPlayerModeDelegate,     // 通过代理获取数据
    WLAudioPlayerModeBuffer,       // 通过缓冲区获取数据
    WLAudioPlayerModeDirect        // 直接提供 PCM 数据
};

@interface WLAudioPlayer : NSObject <WLMediaSourceDelegate>
@property (nonatomic, weak) id<WLAudioPlayerDelegate> delegate;
@property (nonatomic, assign, readonly) WLAudioPlayerMode mode;
@property (nonatomic, assign, readonly) BOOL isPlaying;

- (instancetype)initWithOutputFormat:(AudioStreamBasicDescription)format
                                mode:(WLAudioPlayerMode)mode;
- (BOOL)start;
- (void)stop;
- (void)enqueuePCMData:(const void *)data length:(UInt32)length pts:(Float64)pts;
@end
```

### 2. WLAudioBuffer (新缓冲区)

**位置**: `WorkLabs/MediaSource/WLAudioBuffer.{h,m}`

**设计要点**：
1. 环形缓冲区实现，专为 PCM 音频数据优化
2. 线程安全，使用 `pthread_mutex_t` 和 `pthread_cond_t`
3. 支持流量控制：高低水位线，避免上溢/下溢
4. 时间戳管理：保持音频帧的 PTS 信息
5. 统计信息：缓冲区使用率、丢弃数据量、估计时长

**关键方法**：
```objective-c
- (BOOL)writeData:(const void *)data length:(UInt32)length pts:(Float64)pts;
- (UInt32)readData:(void *)buffer length:(UInt32)length pts:(Float64 *)pts;
- (void)setWatermarkLow:(UInt32)low high:(UInt32)high;
- (BOOL)needsMoreData;   // 低于低水位线
- (BOOL)needsDiscardData; // 高于高水位线
```

### 3. WLResample (重构)

**位置**: `WorkLabs/MediaSource/WLResample.{h,m}`

**主要修复**：
1. **解决第256行缓冲区不足问题**：添加多种处理策略：
   - 错误返回模式：返回 `kWLResampleErrorBufferTooSmall` 错误
   - 安全截断模式：只填充可用空间
   - 多遍处理模式：分多次处理大帧
2. **保留时间同步功能**：保持现有的 `MXTimeSyncMode` 三种时间同步模式（输出严格、输入基准、混合模式）
3. 改进错误处理：定义明确的错误码和恢复策略
4. 添加格式兼容性检查工具

**关键修复**：
```objective-c
- (OSStatus)resampleFrame:(AVFrame *)inputFrame
               outputData:(void **)outputData
            outputSamples:(UInt32 *)outputSamples
           outputCapacity:(UInt32)capacity {

    // 预估所需大小
    int64_t delay = swr_get_delay(_swrContext, _inputSampleRate);
    int64_t estimated = av_rescale_rnd(delay + inputFrame->nb_samples,
                                      _outputSampleRate, _inputSampleRate,
                                      AV_ROUND_UP);
    UInt32 requiredBytes = (UInt32)estimated * _bytesPerOutputFrame;

    // 处理缓冲区不足
    if (requiredBytes > capacity) {
        switch (_resizePolicy) {
            case WLResampleResizePolicyError:
                return kWLResampleErrorBufferTooSmall;
            case WLResampleResizePolicyTruncate:
                return [self safeResampleFrame:inputFrame ...];
            case WLResampleResizePolicyMultiplePass:
                return [self multiPassResampleFrame:inputFrame ...];
        }
    }
    // ... 正常重采样
}
```

### 4. WLMediaSource+AudioPlayer (集成分类)

**位置**: `WorkLabs/MediaSource/WLMediaSource+AudioPlayer.{h,m}`

**目的**：提供便捷的集成方法，保持 `WLMediaSource` 主类简洁

**功能**：
1. 自动连接 `WLAudioPlayer` 作为代理
2. 配置音频格式匹配
3. 管理播放器生命周期

**示例**：
```objective-c
@interface WLMediaSource (AudioPlayer)
@property (nonatomic, strong, nullable) WLAudioPlayer *audioPlayer;
- (BOOL)connectToAudioPlayer:(WLAudioPlayer *)player;
@end
```

## 线程模型

### 线程角色
1. **生产者线程**：
   - `WLMediaSource.audioRenderThread` (解码线程)
   - 外部调用线程 (独立播放器模式)
   - 调用 `WLAudioBuffer.writeData:`

2. **消费者线程**：
   - AudioUnit 渲染回调 (实时线程)
   - 调用 `WLAudioBuffer.readData:`
   - **关键**：不能阻塞，不能分配内存

3. **控制线程**：
   - 主线程或调用线程
   - 播放控制：`start`/`stop`/`pause`
   - 状态查询

### 同步机制
```objective-c
// WLAudioBuffer 内部实现
@interface WLAudioBuffer () {
    pthread_mutex_t _mutex;
    pthread_cond_t _condNotEmpty;  // 消费者等待条件
    pthread_cond_t _condNotFull;   // 生产者等待条件
    UInt8 *_buffer;                // 环形缓冲区
    UInt32 _readPos;
    UInt32 _writePos;
    UInt32 _used;
    NSMutableArray *_ptsBuffer;    // 时间戳队列
}
@end
```

## 错误处理策略

### 错误类型定义
```objective-c
// WLResample 错误
typedef NS_ENUM(NSInteger, WLResampleError) {
    WLResampleErrorNoMemory = -1,
    WLResampleErrorInvalidFormat = -2,
    WLResampleErrorBufferTooSmall = -3,  // 第256行问题
    WLResampleErrorInternal = -4
};

// WLAudioBuffer 错误
typedef NS_ENUM(NSInteger, WLAudioBufferError) {
    WLAudioBufferErrorOverflow = -1,
    WLAudioBufferErrorUnderflow = -2,
    WLAudioBufferErrorInvalidArgument = -3
};
```

### 恢复策略
1. **缓冲区上溢**：丢弃最旧数据或拒绝新数据（可配置）
2. **缓冲区下溢**：填充静音，通知代理需要更多数据
3. **重采样失败**：跳过当前帧，记录错误，继续播放
4. **格式不兼容**：自动创建重采样器，或返回错误

## 性能优化

### 内存管理
1. **预分配**：环形缓冲区预分配，避免实时线程内存分配
2. **内存池**：重采样输出缓冲区复用
3. **零拷贝**：尽可能避免数据复制

### 实时性保证
1. **AudioUnit 配置**：低延迟模式，合适的 I/O 缓冲区大小
2. **缓冲区大小**：根据延迟要求动态计算
3. **线程优先级**：AudioUnit 回调线程设置为实时优先级

### 监控统计
```objective-c
@interface WLAudioMetrics : NSObject
@property (nonatomic, assign) Float64 averageLatency;
@property (nonatomic, assign) UInt32 underrunCount;  // 下溢次数
@property (nonatomic, assign) UInt32 overrunCount;   // 上溢次数
@property (nonatomic, assign) Float64 cpuUsage;
@property (nonatomic, assign) UInt32 resampleErrors; // 重采样错误
@end
```

## 实施步骤

### 阶段1：创建新组件 (保持兼容)
1. 创建 `WLAudioBuffer.{h,m}` - 新环形缓冲区
2. 重构 `WLResample.{h,m}` - 修复缓冲区不足问题
3. 扩展 `WLAudioPlayer.{h,m}` - 添加新接口和模式支持

### 阶段2：逐步集成
1. 修改 `WLAudioPlayer` 使用 `WLAudioBuffer`
2. 创建 `WLMediaSource+AudioPlayer.{h,m}` 集成分类
3. 更新 `WLMediaSource.audioRenderThread` 使用新播放器

### 阶段3：测试和优化
1. 单元测试：缓冲区、重采样器、播放器状态机
2. 集成测试：完整音频链路，不同格式兼容性
3. 性能测试：内存、CPU、延迟
4. 稳定性测试：长时间播放，错误恢复

### 阶段4：清理和文档
1. 移除旧代码（如果完全替换）
2. 更新 API 文档
3. 添加使用示例

## 关键文件路径

1. **新文件**：
   - `WorkLabs/MediaSource/WLAudioBuffer.h`
   - `WorkLabs/MediaSource/WLAudioBuffer.m`
   - `WorkLabs/MediaSource/WLMediaSource+AudioPlayer.h`
   - `WorkLabs/MediaSource/WLMediaSource+AudioPlayer.m`

2. **修改文件**：
   - `WorkLabs/MediaSource/WLAudioPlayer.h` - 扩展接口
   - `WorkLabs/MediaSource/WLAudioPlayer.m` - 实现新功能
   - `WorkLabs/MediaSource/WLResample.h` - 简化接口
   - `WorkLabs/MediaSource/WLResample.m` - 修复第256行问题
   - `WorkLabs/MediaSource/WLMediaSource.m` - 音频渲染线程优化

3. **测试文件**：
   - `WorkLabsTests/WLAudioBufferTests.m`
   - `WorkLabsTests/WLResampleTests.m`
   - `WorkLabsTests/WLAudioPlayerTests.m`

## 验证方案

### 功能验证
1. **基本播放**：播放本地音频文件，验证声音输出
2. **格式兼容**：测试不同采样率、声道数、位深度的音频文件
3. **模式切换**：测试三种播放模式切换
4. **错误恢复**：模拟缓冲区上溢/下溢，验证恢复机制

### 性能验证
1. **延迟测量**：从解码到播放的端到端延迟
2. **CPU使用**：播放期间 CPU 占用率
3. **内存泄漏**：长时间播放后的内存使用
4. **稳定性**：24小时连续播放测试

### 集成验证
1. **WLMediaSource 集成**：播放视频文件，验证音画同步
2. **独立播放器**：直接播放 PCM 数据
3. **代理模式**：自定义数据源播放

## 风险与缓解

### 技术风险
1. **实时线程阻塞**：AudioUnit 回调中不能阻塞
   - **缓解**：使用无锁环形缓冲区设计，快速路径无等待
2. **格式转换精度**：重采样可能引入失真
   - **缓解**：使用高质量重采样算法，添加失真测试
3. **内存管理复杂**：Objective-C 与 C 混合编程
   - **缓解**：明确所有权，使用 RAII 模式，添加内存检测

### 兼容性风险
1. **现有代码依赖**：可能有代码依赖当前接口
   - **缓解**：保持现有接口，逐步迁移，提供适配器
2. **FFmpeg 版本差异**：不同 FFmpeg 版本 API 可能不同
   - **缓解**：使用条件编译，支持多个 FFmpeg 版本

### 性能风险
1. **缓冲区大小不当**：导致延迟过高或上溢/下溢
   - **缓解**：动态缓冲区调整，基于延迟要求自动计算
2. **重采样性能**：复杂格式转换可能消耗 CPU
   - **缓解**：缓存重采样器，避免重复初始化

## 成功标准

1. ✅ 完整音频播放链路：从 FFmpeg 解码到系统输出
2. ✅ 解决 WLResample 第256行缓冲区不足问题
3. ✅ WLAudioPlayer 支持三种使用模式
4. ✅ 实时性能：端到端延迟 < 100ms
5. ✅ 稳定性：24小时连续播放无崩溃
6. ✅ 兼容性：支持常见音频格式（MP3, AAC, FLAC, WAV）
7. ✅ 资源使用：CPU 占用 < 10%，内存无泄漏

## 下一步

基于用户确认的设计决策：
1. ✅ WLResample：保留时间同步功能（MXTimeSyncMode）
2. ✅ 缓冲区：采用环形缓冲区方案
3. ✅ WLAudioPlayer：支持三种使用模式

**待确认事项**：
1. 是否需要支持特定的音频格式或编码？
2. 对向后兼容性的具体要求（哪些现有接口必须保持）？
3. 性能目标的具体数值（延迟、CPU、内存）？

**实施准备**：
如果以上事项确认，可以开始实施阶段1：
1. 创建 `WLAudioBuffer.{h,m}` - 新环形缓冲区
2. 重构 `WLResample.{h,m}` - 修复第256行缓冲区不足问题
3. 扩展 `WLAudioPlayer.{h,m}` - 添加新接口和模式支持

---

*计划创建时间：2026-03-26*
*计划版本：1.0*